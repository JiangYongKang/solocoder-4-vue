你实现的设备授权登录页还存在一些问题，需要进行修复：
1. DeviceAuthorization.vue 中按钮的 :disabled 绑定存在冗余的判断条件，同一个状态被多处重复检查，需要简化。
2. authorizationState.js 中旧的 confirmAuthorization 和 rejectAuthorization 纯函数仍然存在安全隐患，且它们仍作为公开导出的 API 存在，存在未来被误用的风险。需要预防这种误用。
3. 修复后需要确保 pnpm run test 全部通过。
